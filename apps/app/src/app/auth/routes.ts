import { Routes } from "@angular/router";
import { AuthComponent } from "./views/auth/auth-view.component";

export const authRoutes: Routes = [
    {
        path: "", component: AuthComponent
    }
];